export interface Client {
    id: number;
    name: string;
    phone: string;
    image?: string;  // Optional property
    // Add any other properties your clients have
    // email?: string;
    // address?: string;
  }